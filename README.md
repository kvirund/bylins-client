# Bylins MUD Client

Современный клиент для MUD "Былины" на Kotlin + Compose for Desktop.

## Возможности

### Реализовано (v0.2)
- ✅ Современный UI на Compose for Desktop
- ✅ Подключение по Telnet к bylins.su:4000
- ✅ **ANSI цвета** - полная поддержка (16/256/RGB)
- ✅ **MSDP протокол** - автообновление статуса персонажа
- ✅ Панель статуса с реальными данными (HP/Mana/Move)
- ✅ История команд (стрелки вверх/вниз)
- ✅ Отправка пустых строк (Enter)

### В планах
- 🔄 Система триггеров
- 🔄 Система алиасов
- 🔄 **Горячие клавиши** (F1-F12, Ctrl+, Alt+, Numpad)
- 🔄 **Скрипты на Python/Lua/JavaScript/Perl**
- 🔄 Автомаппер
- 🔄 Логирование
- 🔄 Статистика с графиками
- 🔄 Система плагинов (multi-language)

## Требования

- JDK 17 или выше
- Gradle (wrapper включен)

## Установка и запуск

### Разработка

```bash
# Запуск в dev режиме
./gradlew run

# Windows
gradlew.bat run
```

### Сборка дистрибутива

```bash
# Создать установщик для текущей ОС
./gradlew packageDistributionForCurrentOS

# Результат будет в build/compose/binaries/main/
```

## Структура проекта

```
src/main/kotlin/com/bylins/client/
├── Main.kt                      # Точка входа
├── ui/
│   ├── MainWindow.kt            # Главное окно
│   └── components/              # UI компоненты
│       ├── ConnectionPanel.kt   # Панель подключения
│       ├── OutputPanel.kt       # Вывод текста
│       ├── InputPanel.kt        # Ввод команд
│       └── StatusPanel.kt       # Статус персонажа
├── network/
│   ├── TelnetClient.kt          # Telnet клиент
│   └── TelnetParser.kt          # Парсер Telnet протокола
├── triggers/                    # Система триггеров
├── mapper/                      # Автомаппер
├── stats/                       # Статистика
├── logging/                     # Логирование
└── plugins/                     # Система плагинов
```

## Разработка

### Добавление новых зависимостей

Редактируйте `build.gradle.kts`:

```kotlin
dependencies {
    implementation("your.dependency:here:1.0.0")
}
```

### Подключение к серверу

По умолчанию клиент настроен на `mud.bylins.su:4000`. Можно изменить в UI.

### Создание скриптов

Клиент поддерживает скрипты на **Python, Lua, JavaScript и Perl**!

Положите свои скрипты в директорию `scripts/`:

**Python пример** (`scripts/auto_heal.py`):
```python
def on_load(api):
    api.add_trigger(r"HP: (\d+)/(\d+)", check_hp)

def check_hp(api, match):
    hp, max_hp = int(match.group(1)), int(match.group(2))
    if hp < max_hp * 0.3:
        api.send("cast 'cure serious'")
```

**Lua пример** (`scripts/auto_loot.lua`):
```lua
function on_load(api)
    api.add_trigger("^(.+) мертв", on_kill)
end

function on_kill(api, match)
    api.send("взять все " .. match[1])
end
```

Смотрите `scripts/README.md` для полной документации API.

## Лицензия

MIT

# Bylins MUD Client

Современный клиент для MUD "Былины" на Kotlin + Compose for Desktop.

## Возможности

### Реализовано (v0.19)
- ✅ Современный UI на Compose for Desktop
- ✅ Подключение по Telnet к bylins.su:4000
- ✅ **Конфигурируемая кодировка** - UTF-8 (по умолчанию), windows-1251, KOI8-R, ISO-8859-1
  - Выбор кодировки через UI в панели подключения
  - Сохранение в конфигурации
- ✅ **ANSI цвета** - полная поддержка (16/256/RGB), правильная VGA палитра
- ✅ **MSDP протокол** - автообновление статуса персонажа
- ✅ **Система триггеров** - regex паттерны, переменные $1-$9, gag, priority, colorize
- ✅ **Система алиасов** - regex паттерны, переменные $0-$9, priority
- ✅ **Горячие клавиши** - F1-F12, Numpad 0-9, Ctrl+, Alt+, Shift+ модификаторы
- ✅ **UI для триггеров/алиасов/хоткеев** - вкладки, включение/выключение, удаление
- ✅ **Добавление/редактирование через UI** - формы для создания и редактирования
- ✅ **Сохранение/загрузка конфигурации** - автосохранение, импорт/экспорт JSON
- ✅ **Логирование в файл** - старт/стоп, удаление ANSI кодов, очистка старых логов, автозапуск
- ✅ **Статистика сессии** - время, команды, байты, триггеры, алиасы, хоткеи, средние показатели
- ✅ **Графики статистики в реальном времени** - визуализация HP/Mana/Movement
  - Отдельная вкладка "Графики" с тремя графиками
  - История до 300 точек данных (~5 минут)
  - Цветовая дифференциация: HP (красный), Mana (синий), Movement (зелёный)
  - Автоматическое обновление при получении MSDP данных
  - Сетка и текущие значения в процентах
- ✅ **Система переменных** - #var/#unvar/#vars, подстановка @varname и ${varname}, сохранение в config
  - Автоматические: host, port, hp, mana, movement и все MSDP переменные
  - Сохранение из триггеров: `#var target $1` (сохраняет regex группу)
- ✅ **Команда #help** - встроенная справка по всем доступным командам
- ✅ **Звуковые уведомления** - система аудио оповещений о важных событиях
  - Автоматические уведомления: низкое HP (< 30%), получение tell/whisper
  - Команда #sound для проигрывания звуков: tell, whisper, lowhp, levelup, death, combat, alert, beep
  - Управление громкостью и включение/выключение
  - Стандартные триггеры для tell/whisper с звуковыми уведомлениями
- ✅ **Эхо команд в лог** - все отправленные команды видны в выводе
- ✅ **Стандартные триггеры, алиасы и хоткеи** - ready-to-use примеры
- ✅ **Оптимизация памяти** - агрессивная очистка буферов и кеширование объектов
  - Главный буфер ограничен 1 МБ (вместо 5 МБ)
  - Вкладки ограничены 1000-2000 строк (вместо 10000)
  - История команд ограничена 500 командами
  - Оптимизированное обновление UI (меньше создаётся временных объектов)
  - **SpanStyle кеширование** - критичное исправление утечки памяти в ANSI парсере
  - История графиков ограничена 300 точками данных (~5 минут)
- ✅ Панель статуса с реальными данными (HP/Mana/Move)
- ✅ **Мини-карта на главной вкладке** - компактное отображение окружающих комнат
  - Радиус обзора 4 клетки в каждую сторону
  - Цветовое кодирование: текущая (зелёная), посещённая (синяя), непосещённая (жёлтая)
  - Отображение выходов из комнат
  - Информация о текущей комнате и координаты
- ✅ История команд (стрелки вверх/вниз, курсор в конце)
- ✅ Отправка пустых строк (Enter)
- ✅ **Система вкладок** - redirect текста по паттернам, copy/move/only режимы, UI с табами
  - Стандартная вкладка "Каналы" для сообщений (говорит, шепчет, болталка)
  - Фильтрация текста по regex паттернам
  - Исправлена прокрутка при переключении вкладок
  - **UI для управления вкладками** - создание/редактирование/удаление через интерфейс
- ✅ **Автомаппер** - автоматическое построение карты
  - **Улучшенный парсинг** - поддержка множества форматов выходов, автоопределение типа местности
  - Парсинг названия, описания и выходов из текста и MSDP
  - Координатная система (x, y, z) с поддержкой уровней
  - Визуализация на Canvas: drag для перемещения, zoom, переключение уровней
  - Визуальные индикаторы текущей/посещенной комнаты
  - **Цветовое кодирование комнат** - 9 предустановленных цветов
  - **Заметки для комнат** - с визуальным индикатором
  - **Групповые метки (теги)** - маркировка комнат по категориям (магазин, тренер, квест и т.д.)
  - **Автоматическая детекция зон** - интеллектуальное определение зон на основе названий и расположения комнат
  - Клик по комнате открывает диалог редактирования
  - **База данных карт (SQLite)** - хранение множества карт с метаданными
    - Сохранение/загрузка карт по имени
    - Список всех сохраненных карт с информацией
    - Удаление карт из базы
    - Автоматическая база в ~/.bylins-client/maps/maps.db
  - **Экспорт/импорт карт** в JSON формате
  - **Автосохранение** - карта автоматически сохраняется/загружается при подключении
  - **Pathfinding (поиск пути)** - A* алгоритм (эвристический) и BFS для навигации
  - **Поиск комнат** - поиск по имени/описанию/заметкам
  - **Команды навигации**:
    - `#goto <room_id>` - автоматический переход к указанной комнате
    - `#run` - автоматический переход к ближайшей непосещенной комнате
    - `#find <название>` - поиск комнат по названию с автопереходом
    - **Speedwalk** - быстрое перемещение: `5n`, `3n2e`, `10sw` (встроенная поддержка)
  - Управление: включение/выключение, очистка
- ✅ **Система скриптов** - мультиязычная поддержка
  - **JavaScript** - Nashorn standalone 15.4 (полная поддержка для JDK 15+)
  - **Python** - Jython 2.7 (полная поддержка)
  - **Lua** - LuaJ 5.2 (полная поддержка)
  - **Perl** - внешний процесс (обнаружение доступно, полная интеграция в разработке)
  - События: on_load, on_connect, on_disconnect, on_line, on_command, on_msdp, on_trigger, on_alias, on_room_enter
  - API: send(), echo(), addTrigger(), addAlias(), setTimeout(), setInterval(), getVariable(), setVariable()
  - MSDP API: getMsdpValue(), getAllMsdpData()
  - Mapper API: getCurrentRoom(), getRoomAt(), setRoomNote(), setRoomColor()
  - Автозагрузка скриптов из директории scripts/
  - UI для управления: загрузка, выгрузка, включение/выключение, перезагрузка
  - **Готовые примеры** в scripts/examples/:
    - test.js/py/lua - демонстрация всех событий
    - auto_heal.js/py/lua - автолечение при низком HP
    - auto_loot.js - автосбор лута
    - combat_stats.js - статистика боя
    - speedwalk.js - быстрое перемещение (#5n2e)
  - Подробная документация в scripts/README.md

- ✅ **Экспорт/импорт настроек** - обмен конфигурациями между пользователями
  - Экспорт триггеров, алиасов и хоткеев в JSON формат
  - Импорт с поддержкой merge (добавление) и replace (замена) режимов
  - Выборочный экспорт - можно экспортировать только выбранные элементы
  - **UI для экспорта/импорта** - кнопки в панелях триггеров, алиасов и хоткеев
  - API методы: exportTriggers(), importTriggers(), exportAliases(), importAliases(), exportHotkeys(), importHotkeys()

- ✅ **Темы оформления** - настраиваемый внешний вид интерфейса
  - 5 предустановленных тем: Тёмная, Светлая, Тёмно-синяя, Solarized Dark, Monokai
  - Настройка через UI в панели настроек
  - Сохранение выбранной темы в конфигурации
  - Material Design 3 интеграция
  - Примечание: изменение темы требует перезапуска приложения

- ✅ **Настройка шрифтов** - полная кастомизация текста игры
  - Выбор семейства шрифта: Monospace, Serif, Sans Serif, Cursive
  - Настройка размера шрифта (10-24 sp)
  - Применение в реальном времени к игровому выводу
  - Автоматическая адаптация межстрочного интервала
  - Сохранение настроек в конфигурации

### В планах
- 🔄 Система плагинов (multi-language)

## Требования

- JDK 17 или выше
- Gradle (wrapper включен)

## Установка и запуск

### Разработка

```bash
# Запуск в dev режиме
./gradlew run

# Windows
gradlew.bat run
```

### Сборка дистрибутива

```bash
# Создать установщик для текущей ОС
./gradlew packageDistributionForCurrentOS

# Результат будет в build/compose/binaries/main/
```

## Структура проекта

```
src/main/kotlin/com/bylins/client/
├── Main.kt                      # Точка входа
├── ui/
│   ├── MainWindow.kt            # Главное окно
│   └── components/              # UI компоненты
│       ├── ConnectionPanel.kt   # Панель подключения
│       ├── OutputPanel.kt       # Вывод текста
│       ├── InputPanel.kt        # Ввод команд
│       └── StatusPanel.kt       # Статус персонажа
├── network/
│   ├── TelnetClient.kt          # Telnet клиент
│   └── TelnetParser.kt          # Парсер Telnet протокола
├── triggers/                    # Система триггеров
├── mapper/                      # Автомаппер
├── stats/                       # Статистика
├── logging/                     # Логирование
└── plugins/                     # Система плагинов
```

## Разработка

### Добавление новых зависимостей

Редактируйте `build.gradle.kts`:

```kotlin
dependencies {
    implementation("your.dependency:here:1.0.0")
}
```

### Подключение к серверу

По умолчанию клиент настроен на `mud.bylins.su:4000`. Можно изменить в UI.

### Создание скриптов

Клиент поддерживает скрипты на **JavaScript, Python, Lua и Perl**!

Положите свои скрипты в директорию `scripts/`:

**JavaScript пример** (`scripts/auto_heal.js`):
```javascript
var HEAL_THRESHOLD = 30;

function on_msdp(data) {
    var hp = api.getMsdpValue("HEALTH");
    var maxHp = api.getMsdpValue("HEALTH_MAX");

    if (hp && maxHp) {
        var hpPercent = (hp / maxHp) * 100;
        if (hpPercent < HEAL_THRESHOLD) {
            send("cast 'cure serious'");
        }
    }
}
```

**Python пример** (`scripts/auto_heal.py`):
```python
HEAL_THRESHOLD = 30

def on_msdp(data):
    hp = api.getMsdpValue("HEALTH")
    max_hp = api.getMsdpValue("HEALTH_MAX")

    if hp and max_hp:
        hp_percent = (float(hp) / float(max_hp)) * 100
        if hp_percent < HEAL_THRESHOLD:
            send("cast 'cure serious'")
```

**Lua пример** (`scripts/auto_heal.lua`):
```lua
local HEAL_THRESHOLD = 30

function on_msdp(data)
    local hp = api:getMsdpValue("HEALTH")
    local max_hp = api:getMsdpValue("HEALTH_MAX")

    if hp and max_hp then
        local hp_percent = (tonumber(hp) / tonumber(max_hp)) * 100
        if hp_percent < HEAL_THRESHOLD then
            send("cast 'cure serious'")
        end
    end
end
```

Смотрите `scripts/README.md` для полной документации API.

## Лицензия

MIT
